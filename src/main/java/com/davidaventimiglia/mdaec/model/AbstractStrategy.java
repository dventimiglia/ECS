package com.davidaventimiglia.mdaec.model;

import java.util.*;

/**
 * Data type for an ElevatorControlSystem strategy.
 *
 * A strategy encapsulates the logic for allocating routes to
 * elevators.
 */
public abstract class AbstractStrategy {
    /**
     * Run the calculation to allocate routes to elevators.
     */
    public void calculate (AbstractElevatorControlSystem ecs) {
	return;}}		// no-op
    
